%legend
  .row-fluid
    .pull-left
      %h2 Ideas
    .pull-right
      %a.btn.btn-primary{href: new_user_idea_path(current_user)}
        %i.icon-plus.icon-white
        New Idea


- @ideas.each do |idea|
  .well
    %b= idea.title
    %div
      = idea.description

.pagination.pagination-centered
  = paginate @ideas
