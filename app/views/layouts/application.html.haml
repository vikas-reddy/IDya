!!!
%html
  %head
    %title IDya
    = stylesheet_link_tag    "application", :media => "all"
    = javascript_include_tag "application"
    = csrf_meta_tags
  %body
    .navbar
      .navbar-inner
        .container-fluid
          %a.btn.btn-navbar{"data-target" => ".nav-collapse", "data-toggle" => "collapse"}
            %span.icon-bar
            %span.icon-bar
            %span.icon-bar
          %a.brand{:href => "/"} IDya
          .btn-group.pull-right
            %a.btn.dropdown-toggle{"data-toggle" => "dropdown", :href => "#"}
              %i.icon-user
              = current_user
              %span.caret
            %ul.dropdown-menu
              - if logged_in?
                %li= link_to 'Sign Out', signout_path, method: :delete
              - else
                %li= link_to 'Sign In', signin_path
          .nav-collapse
            %ul.nav.pull-left
              %li= link_to 'Home', root_path
            %ul.nav.pull-right
              %li
                %form.navbar-form.pull-right{action: root_path}
                  %input.span3{type: :text, name: 'q', placeholder: 'Search Ideas'}
          / /.nav-collapse
    .container
      .hero-unit
        = render 'shared/flashes'
        = yield
      %hr/
      %footer
        %p &copy; Copyright.Imaginea.2012.

